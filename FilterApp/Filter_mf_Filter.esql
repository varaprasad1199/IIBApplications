

CREATE FILTER MODULE Filter_mf_Filter
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE sName CHARACTER;
		SET sName = UCASE(Root.XMLNSC.student.name);

	IF sName='RANJITH' THEN
		RETURN TRUE;
	ELSEIF sName='Ram' THEN
		RETURN	FALSE;
	ELSE
		RETURN UNKNOWN;
	END IF;
	END;

END MODULE;
